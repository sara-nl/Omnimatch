# This is a job requesting 12 cores
#
# request 2 nodes, 2 cores per node and 
#  each node equipped with the infiniband network:
#PBS -lnodes=2:ppn=6
# job requires at most 1 hour wallclock time
#PBS -lwalltime=1:00:00

cd $HOME/omnimatch/bin

# load the necessary module(s), for example:
#module load paffinity
module load fftw2/openmpi/intel
module load openmpi/intel
module load c
#module load gperftools

# determine the number of processors:
#nprocs=`wc -l < $PBS_NODEFILE`
# determine the number of nodes:
#nnodes=`sort $PBS_NODEFILE | uniq | wc -l`

# start your program
cat $PBS_NODESFILE
time mpiexec ./omnimatch ../data/testvol.em ../data/templ.em testvol12 0 180 20 0 180 20 0 180 20 ../data/psf.em ../data/mask_15.em 128 >testvol12.out 2>testvol12.err
#mpiexec ./omnimatch.exe
